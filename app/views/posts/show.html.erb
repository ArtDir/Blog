<h1><%= @post.title %></h1>
<div class="content">
	<%= @post.body %>
</div>
<br>
<%= link_to "Редактировать", edit_post_path(@post) %> <br>
<%= link_to "Удалить", post_path(@post), method: :delete, data: { confirm: "Уверен?" } %>